#!/usr/bin/env python3
"""vibec, run straight from a checkout. Installed copies use the console
script entry point in pyproject.toml instead."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from vibelang.cli import main   # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
